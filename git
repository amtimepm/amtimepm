😋   * runs-on: 
     ³{{ (!matrix.language == '#F' && '-sub-system') 
     # ||'-cuda' && '-blue' ;}}

🤠   * timeout-minutes:
     ²!{{ (matrix.language == 'f' && 16) 
     # || 2 }}

🗣️    * permissions: [ 0$ ]
      * actions: read \ write
      * contents: read
      * security-events: write

🧬    * strategy: [ - ]
      * fail-fast: false
      * fraud-fail: true

!     * .0.1.2: [ ~binomial ]