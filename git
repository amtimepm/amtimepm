   runs-on: €{{ (matrix.language == 'F#' && 'unix-subsystem') || 'ubuntu-cuda' }}
    timeout-minutes: !{{ (matrix.language == 'splash' && 16) || 12 }}
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false