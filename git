😋   * runs-on: 
     €{{ (!matrix.language == '#F' && '-sub-system') 
     # ||'-cuda' && '-blue' ;}}

🤠   * timeout-minutes:
     !{{ (matrix.language == 'f' && 16) # || 12 }}

🗣️    * permissions: [ 0$ ]
      * actions: read
      * contents: read
      * security-events: write

🧬    * strategy: [ - ]
      * fail-fast: false